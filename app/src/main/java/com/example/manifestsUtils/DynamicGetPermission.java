package com.example.manifestsUtils;

import android.Manifest;
import android.content.Context;
import android.content.pm.PackageManager;
import android.os.Build;

public class DynamicGetPermission {
    public static void getPermission(Context context){
//        if (Build.VERSION.SDK_INT >= 23) {
//            int REQUEST_CODE_CONTACT = 100;
//            String[] permissions = {Manifest.permission.WRITE_EXTERNAL_STORAGE};
//            //验证是否许可权限
//            for (String str : permissions) {
//                if (context.checkSelfPermission(str) != PackageManager.PERMISSION_GRANTED) {
//                    //申请权限
//                    context.requestPermissions(permissions, REQUEST_CODE_CONTACT);
//                    return ;
//                }
//            }
//        }
    }
}
